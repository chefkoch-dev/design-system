import {  Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { createButton } from './Button';

<Meta title="Library/Buttons/Primary" 
  argTypes={{
    label: { control: 'text' },
    mode: {
      control: { type: 'select', options: ['primary', 'secondary', 'tertiary', 'danger', 'conversion'] },
    },
  }}
/>

export const Template = ({ label, ...args }) => {
  //You can either use a function to create DOM elements or use a plain html string!
  // return `<div>${label}</div>`;
  //return createButton({ label, ...args });
  return `<button class="ds-btn ds-btn-${args.mode}"><span class="ds-btn__label">${label}</span></button>`;
};

export const TemplateIcon = ({ label, ...args }) => {
  //You can either use a function to create DOM elements or use a plain html string!
  // return `<div>${label}</div>`;
  //return createButton({ label, ...args });
  return `<button class="ds-btn ds-btn-${args.mode}"><i class="chefkoch-icons">heart</i><span class="ds-btn__label">${label}</span></button>`;
};


export const TemplateIconOnly = ({ ...args }) => {
  //You can either use a function to create DOM elements or use a plain html string!
  // return `<div>${label}</div>`;
  //return createButton({ label, ...args });
  return `<button class="ds-btn ds-btn-${args.mode}"><i class="chefkoch-icons">heart</i></button>`;
};


# Buttons
## Primary Button

Der Primary Button ist reserviert für die Main-CTA einer Seite, bzw. eines Bereiches.

### Usage  
    
    Via Shortlink 
    @import "~chefkoch-design-system/components/button-primary";
    
    Original Location
    @import "~chefkoch-design-system/app/components/buttons/buttons-primary";


<Canvas>
  <Story name="Text only" args={{
    mode: 'primary', 
    label: 'Button'
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="with Icon" args={{
    mode: 'primary', 
    label: 'Button'
  }}>
    {TemplateIcon.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Icon only" args={{
    mode: 'primary', 
  }}>
    {TemplateIconOnly.bind({})}
  </Story>
</Canvas>